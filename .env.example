# Railway 환경변수 예시 파일
# 실제 배포시에는 Railway 대시보드에서 설정하세요

# 데이터베이스 연결 URL (Railway PostgreSQL 서비스에서 자동 생성)
DATABASE_URL=postgresql://username:password@host:port/database

# 세션 암호화 키 (강력한 임의 문자열로 설정)
SESSION_SECRET=your-super-secure-session-secret-key-here

# Flask 환경 (프로덕션 배포시)
FLASK_ENV=production

# Python 경로 (선택사항)
PYTHONPATH=/app